<UserControl x:Class="AIsle.DesktopApp.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="8">
            <!-- Stats Cards -->
            <UniformGrid Columns="3" Margin="0,0,0,20">
                <materialDesign:Card Margin="8" Padding="20">
                    <StackPanel>
                        <TextBlock Text="Tổng số Phiên chạy" FontSize="13" Opacity="0.7" />
                        <TextBlock Text="{Binding TotalRuns}" FontSize="36" FontWeight="Bold" Foreground="{StaticResource BlueBrush}" />
                    </StackPanel>
                </materialDesign:Card>
                <materialDesign:Card Margin="8" Padding="20">
                    <StackPanel>
                        <TextBlock Text="Doanh thu Trung bình" FontSize="13" Opacity="0.7" />
                        <TextBlock Text="{Binding AvgRevenue}" FontSize="36" FontWeight="Bold" Foreground="{StaticResource AmberBrush}" />
                    </StackPanel>
                </materialDesign:Card>
                <materialDesign:Card Margin="8" Padding="20">
                    <StackPanel>
                        <TextBlock Text="Tỷ lệ Chuyển đổi TB" FontSize="13" Opacity="0.7" />
                        <TextBlock Text="{Binding AvgConversion}" FontSize="36" FontWeight="Bold" Foreground="#4CAF50" />
                    </StackPanel>
                </materialDesign:Card>
            </UniformGrid>

            <!-- Recent Runs -->
            <materialDesign:Card Margin="8" Padding="20">
                <StackPanel>
                    <TextBlock Text="📜 Phiên chạy gần nhất" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12" />
                    <DataGrid ItemsSource="{Binding RecentRuns}" AutoGenerateColumns="False"
                              IsReadOnly="True" CanUserAddRows="False" HeadersVisibility="Column"
                              materialDesign:DataGridAssist.CellPadding="12 8" Margin="0,4,0,0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Tên phiên" Binding="{Binding Name}" Width="*" />
                            <DataGridTextColumn Header="Ngày chạy" Binding="{Binding Date}" Width="180" />
                            <DataGridTextColumn Header="Doanh thu" Binding="{Binding Revenue}" Width="140" />
                            <DataGridTextColumn Header="Lượt mua" Binding="{Binding Purchases}" Width="100" />
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </materialDesign:Card>
        </StackPanel>
    </ScrollViewer>
</UserControl>
